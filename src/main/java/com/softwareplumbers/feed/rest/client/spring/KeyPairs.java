package com.softwareplumbers.feed.rest.client.spring;

/** Enumerate the public/private key pairs used by this package */
public enum KeyPairs {
    /** Key pair for the default service account */
    DEFAULT_SERVICE_ACCOUNT
}
